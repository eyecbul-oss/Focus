{
  "name": "sezr-focus",
  "version": "1.0.0",
  "private": true,
  "description": "SezR Focus Android/iOS app",
  "scripts": {
    "android:debug": "npx cap sync android && cd android && ./gradlew assembleDebug",
    "android:aab": "npx cap sync android && cd android && ./gradlew bundleRelease",
    "ios:sync": "npx cap sync ios"
  },
  "dependencies": {
    "@capacitor/android": "^6.1.2",
    "@capacitor/core": "^6.1.2",
    "@capacitor/ios": "^6.1.2"
  },
  "devDependencies": {
    "@capacitor/cli": "^6.1.2"
  }
}