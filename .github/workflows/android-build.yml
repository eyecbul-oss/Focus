name: Build SezR Focus Expo Android

on:
  workflow_dispatch:

jobs:
  disabled:
    runs-on: ubuntu-latest
    steps:
      - name: Disabled
        run: echo "Expo workflow disabled. Local Focus APK is built by Android CI."
