import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.sezr.focus',
  appName: 'SezR Focus',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
