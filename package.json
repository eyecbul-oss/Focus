{
  "name": "sezr-focus-standalone",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "android:debug": "npx cap add android || true && npx cap sync android && cd android && ./gradlew assembleDebug",
    "android:aab": "npx cap add android || true && npx cap sync android && cd android && ./gradlew bundleRelease"
  },
  "dependencies": {
    "@capacitor/android": "^6.1.2",
    "@capacitor/core": "^6.1.2",
    "@capacitor/ios": "^6.1.2"
  },
  "devDependencies": {
    "@capacitor/cli": "^6.1.2"
  }
}